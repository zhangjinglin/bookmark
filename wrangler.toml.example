name = "bookmark-worker"
main = "src/index.ts"
compatibility_date = "2024-01-17"

# 支持导入 HTML 文件
rules = [{ type = "Text", globs = ["*.html"] }]

# KV 命名空间绑定
# 运行以下命令获取 ID:
# pnpm wrangler kv:namespace create "BOOKMARKS"
[[kv_namespaces]]
binding = "BOOKMARKS"
id = "YOUR_KV_NAMESPACE_ID"
